uuid: fced0fab-d073-4dd6-bb13-773e7d49923e
langcode: ru
status: true
dependencies:
  config:
    - field.storage.node.field_birthyearclass
    - node.type.openpowerliftingstring
id: node.openpowerliftingstring.field_birthyearclass
field_name: field_birthyearclass
entity_type: node
bundle: openpowerliftingstring
label: BirthYearClass
description: "Optional. The birth year class in which the filter falls, for example `40-49`. The ages in the range are the oldest possible ages for the lifter that year. For example, `40-49` means \"the year the lifter turns 40 through the full year in which the lifter turns 49.\"\r\n\r\n`BirthYearClass` is used primarily by the IPF and by IPF affiliates. Non-IPF federations tend to use `AgeClass` instead.\r\n\r\nThe full range available to `BirthYearClass` is defined by [modules/opltypes/src/birthyearclass.rs](https://gitlab.com/openpowerlifting/opl-data/blob/master/modules/opltypes/src/birthyearclass.rs). \r\n"
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
